In this example you see table with multiple spans across 3 table rows.
Ideally, a tool would not perform a page break inbetween a row span.
`td.rowspan {page-break-inside: avoid}` is only respected by Antennahouse.
