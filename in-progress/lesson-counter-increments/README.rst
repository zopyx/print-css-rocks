This test renders three chapters on three different pages for testing
incrementing counters. The page counter is initially set to 2 and incremented
by 2 on every subsequent page. The chapter counter starts with 3 and is
incremented by 3 on every subsequent page.
